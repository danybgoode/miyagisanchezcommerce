# Routine A trigger test

Verifies Routine A auto-fires on `pull_request.opened` after the trigger was flipped from
`ready_for_review` to `opened`. Throwaway — close + delete once the advisory comment auto-appears.
